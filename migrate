#!/usr/bin/env bash
set -euo pipefail

ROOT="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
cd "$ROOT"

exec docker run --rm -it \
  --entrypoint python \
  --workdir /scripts \
  --env PYTHONPATH=/scripts/src \
  -v "$ROOT:/scripts" \
  juniper/pyez \
  -m ex_migration_operator.cli "$@"
